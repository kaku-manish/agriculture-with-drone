name: Deploy Frontend to GitHub Pages

# Request permissions so the workflow token can push the gh-pages branch
permissions:
  contents: write
  pages: write
  id-token: write

on:
  push:
    branches: [ "main" ]

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          # ensure the GITHUB_TOKEN is available for subsequent pushes
          persist-credentials: true

      - name: Use Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'

      - name: Install and build client
        working-directory: ./client
        run: |
          npm ci
          npm run build

      - name: Show build output (diagnose)
        run: |
          echo "Listing client/dist contents"
          ls -la client/dist || true

      - name: Deploy to GitHub Pages
        uses: peaceiris/actions-gh-pages@v3
        with:
          # Prefer a user-provided PAT (GH_PAGES_PAT). If not set, fall back to GITHUB_TOKEN.
          github_token: ${{ secrets.GH_PAGES_PAT || secrets.GITHUB_TOKEN }}
          publish_dir: ./client/dist
